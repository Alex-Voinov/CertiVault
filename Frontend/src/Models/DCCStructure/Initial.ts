interface IInitional {
    sig?: string; // название файла подписи
    comment?: string[]; // названия файлов комментарие
    document?: number; //индекс созданного документа
}

export default IInitional